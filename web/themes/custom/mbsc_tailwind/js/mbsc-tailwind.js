/**
 * @file
 * mbsc_tailwind behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.mbscTailwind = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
